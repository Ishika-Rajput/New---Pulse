export default function ClusterPage({ params }) {
  return (
    <main style={{ padding: 40 }}>
      <h1>Cluster {params.id}</h1>
      <p>Cluster article details will appear here.</p>
    </main>
  );
}
